<%= form_for(current_user.spots.find_by_restaurant_id(@restaurant.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Remove spot", class: "btn btn-large" %>
<% end %>