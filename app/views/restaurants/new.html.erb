<% provide(:title, 'Add a restaurant') %>
<h1>Add a restaurant</h1>

<div class="row">
  <div class="span6">
    <%= form_for(@restaurant) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Add restaurant", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>